# Dependencies are bounded (floor + upper major) rather than open >= so a new
# major release cannot silently change behavior in CI or at deploy time. This
# addresses supply-chain reproducibility for a security-critical tool and
# prevents the class of breakage where an unpinned tool (e.g. ruff) enables new
# default rules on a new release. Verified-installed versions are pinned to the
# tested minor; the rest use compatible-release upper bounds.

# Core
aws-cdk-lib>=2.244.0,<3.0.0
constructs>=10.5.1,<11.0.0
boto3>=1.42.0,<2.0.0

# Testing
pytest>=8.0.0,<10.0.0
pytest-cov>=5.0.0,<7.0.0
moto[dynamodb,s3,sns,cloudwatch,lambda,stepfunctions]>=5.0.0,<6.0.0

# Linting and Type Checking
ruff>=0.4.0,<1.0.0
mypy>=1.10.0,<2.0.0
boto3-stubs[dynamodb,s3,sns,cloudwatch,lambda,stepfunctions]>=1.28.0,<2.0.0

# Security scanning
bandit>=1.7.0,<2.0.0
pip-audit>=2.7.0,<3.0.0
detect-secrets>=1.5.0,<2.0.0
